# Component Skeleton: users

Use this folder as the planning map for future domain components in `apps/web/components/domain/users/**`.

Do not add shared UI primitives from this lane unless the task explicitly includes a cross-team UI primitive PR.
